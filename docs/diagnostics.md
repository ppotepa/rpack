# Diagnostics

Diagnostics aim to answer one question: why did a package fail, and what should the user do next?

Two main surfaces exist:

- standard human-readable output
- `--json` output for automation

Agent conflict output also supports an LLM-oriented report for repair workflows.
